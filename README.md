# payload-tree-relationships

Example repository to reproduce an SQL error relating to Payload relationships/joins. See the TreeBranchNode configuration to see the field configuration that causes the error
